<nav>
    <ul>
        <li><a href="{{ path('main_home') }}"title="Home">Acceuil</a></li>
        <li><a href="#" title="Mon profil">Mon Profil</a></li>

        {% if app.user %}
            <li><a href="#" title="Créer une nouvelle sortie">Créer une Sortie</a></li>
            <li><a href="{{ path('app_logout') }}" title="Logout">Logout ({{ app.user.prenom}})</a></li>

            {% if is_granted("ROLE_ADMIN") %}
                <li><a href="#" title="Admin dashboard">admin</a></li>

            {% endif %}

        {% else %}
            <li><a href="{{ path('app_login') }}" title="Login">Login</a></li>
            <li><a href="{{ path('app_register') }}" title="Create account">Register</a></li>

        {% endif %}
    </ul>
</nav>