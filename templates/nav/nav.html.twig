        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" aria-current="page" href="{{ path('main_home') }}"title="Home">Accueil</a>
                        </li>
                        {% if app.user %}

                            <li class="nav-item"><a class="nav-link" href="{{ path('profil_profil') }}" title="Mon profil">Mon Profil</a></li>
                            <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}" title="Logout">Logout ({{ app.user.pseudo}})</a></li>

                            {% if is_granted("ROLE_ADMIN") %}
                                <li class="nav-item"><a class="nav-link" href="#" title="Admin dashboard">admin</a></li>

                            {% endif %}

                        {% else %}
                            <li class="nav-item"><a class="nav-link" href="{{ path('app_login') }}" title="Login">Login</a></li>
                            <li class="nav-item"><a class="nav-link" href="{{ path('app_register') }}" title="Create account">Register</a></li>

                        {% endif %}

                    </ul>
                </div>
            </div>
        </nav>
